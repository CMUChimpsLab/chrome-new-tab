type Question {
  _id: ID!
  title: String!
  description: String
  options: [Option]
}

extend type Mutation {
  createQuestion(
    title: String!
    description: String
    options: [String]
  ): Question
  deleteQuestion(_id: ID!): Question
}

extend type Query {
  questions: [Question]
}
